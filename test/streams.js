const test = require('tape')
const times = require('../times')
const sleep = require('../async/sleep')
const {
  pipe,map,fromIterator,each
} = require('../streams')

test('streams',t=>{
  t.test('pipe',t=>{
  })
})
